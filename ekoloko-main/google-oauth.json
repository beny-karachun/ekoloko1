{
  "_comment": "Google OAuth client for sign-in-with-Google. Create a 'Desktop app' OAuth client in Google Cloud Console (APIs & Services > Credentials), then paste the values here. Per Google's docs the desktop-app secret is not confidential, so this file ships with the app. YOUR_* placeholders keep the feature disabled.",
  "clientId": "YOUR_CLIENT_ID.apps.googleusercontent.com",
  "clientSecret": "YOUR_CLIENT_SECRET"
}
